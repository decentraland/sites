import { Typography, styled } from 'decentraland-ui2'

const SceneTitle = styled(Typography)({
  maxWidth: 220,
  fontWeight: 600
})

const SceneLocation = styled(Typography)({
  display: 'block',
  maxWidth: 240
})

export { SceneLocation, SceneTitle }
