// Intentionally empty: the place detail modal now delegates Paper + Hero +
// Content to whats-on/DetailModal/DetailModal.styled.ts (StyledDialog) and
// whats-on/PlaceDetailModal/{PlaceDetailModalHero,PlaceDetailModalContent}.
// The legacy styled components (PlaceDetailDialog, PlaceCoverImage,
// PlaceDetailRoot, …) were removed; see git history if revival is needed.

export {}
