import { Box, styled } from 'decentraland-ui2'

const CreateLoadingContainer = styled(Box)({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  minHeight: '100vh',
  backgroundColor: '#18141c'
})

export { CreateLoadingContainer }
