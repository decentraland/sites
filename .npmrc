# Third-party dependencies are pinned (see CLAUDE.md > Dependencies). Without this, a plain
# `npm install <pkg>` writes a caret range and the pin is lost silently.
# Decentraland packages are the exception and keep `^`: install them with
# `npm install --save-prefix='^' @dcl/<pkg>`.
save-exact=true
